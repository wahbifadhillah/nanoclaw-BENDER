
# Anthropic API key (used when not routing via OpenRouter)
ANTHROPIC_API_KEY=

# OpenRouter API key — used by the LiteLLM proxy to route to non-Anthropic models.
# Takes priority over ANTHROPIC_API_KEY when set.
OPENROUTER_API_KEY=
